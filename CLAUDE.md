# シミュレーター完成LOOP定義

このセクションを既存の CLAUDE.md に追記してください。
`/loop` 実行時、Claude Code は以下を厳守すること。

## 目的

プラレールコース生成シミュレーターの以下4層を、テスト駆動で自動修正する。
人間の目視評価ではなく、機械的に判定可能なテストの pass/fail を進捗の唯一の基準とする。

- 幾何整合性(閉ループ・非重複・パーツ形状の単一定義)
- 生成ロジック(バリエーション・分布)
- 描画(枕木・ジョイント等の視覚要素)
- 走行(弧長ベースの位置管理・分岐進路選択)

## 実行手順(1反復)

1. `pytest tests/test_geometry.py -v` および `flutter test test/simulator_render_test.dart -v`
   を実行し、失敗しているテストを全て列挙する
2. 失敗テストのうち **最も根本原因に近いもの1つだけ** を選ぶ。
   優先順位: 幾何整合性 > 生成ロジック > 描画 > 走行
   (上位層が原因で下位層のテストが落ちている場合は、下位層を直す前に上位層から直すこと)
3. 選んだテストに対応する修正方針を3行以内で述べ、変更対象ファイルを列挙する。
   この時点でファイルはまだ変更しない。
4. 列挙したファイルのみ変更する。無関係なファイル・無関係なテストは一切触らない。
5. 該当テストのみ再実行し、pass したか報告する。
   (全テストの再実行はステップ6の最終確認時のみ)
6. 全テストスイートを実行する。
   - 全てpass → 完了報告して停止
   - 未passのテストが残る → 手順1に戻る
   - 5反復を超えても未解決 → 停止し、残存する失敗テストと
     これまで試した修正内容の一覧を人間向けにまとめて報告する

## 禁止事項(重要)

- テストの期待値・閾値・許容誤差を、実装を直す代わりに緩めて通すこと
  (例: 閉ループの誤差許容を10mmから50mmに緩める、などは不可)
- 1反復で複数の失敗テストを同時に修正すること(原因の切り分けが不能になるため)
- `rail_db.py`(パーツ形状の定義)と `algorithm.py`(生成ロジック)の
  座標変換ロジックを同一反復内で両方変更すること
  (どちらの変更が効いたか判定できなくなるため、片方ずつ)
- 分岐(`switch_*`, `cross_point`)関連のロジックを、方針決定前に触ること
  (別スコープとして扱う)

## 反復ログの残し方

各反復の最後に、以下の形式で1行サマリーを `LOOP_LOG.md` に追記する。

```
[反復N] 対象テスト: <test名> / 変更ファイル: <path> / 結果: pass|fail / 次の懸念: <あれば>
```

これにより、後から「何回目にどこを触ったら何が直ったか」を人間が追跡できるようにする。


## 実行環境ノート（このリポジトリ固有・追記）

- Python テスト: `backend_azure_functions\.venv\Scripts\python.exe -m pytest tests/test_geometry.py -v`
  (venv の pytest を使用。リポジトリルートから実行)
- Flutter テスト: ローカルに Flutter SDK が無いため、GitHub Actions の
  `Flutter Tests` ワークフロー (`gh workflow run "Flutter Tests"`) で実行し、
  `gh run watch` で pass/fail を判定する
