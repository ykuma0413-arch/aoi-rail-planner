# 📱 スマホ実機検証ガイド (Android)

PCに Flutter SDK を入れずに、クラウドビルドで生成した APK を Android 端末にインストールして検証します。

---

## 全体の流れ

```
[1] GitHub アカウント準備     ← 1回だけ
       ↓
[2] gh auth login (PCで認証)  ← 1回だけ
       ↓
[3] push-to-github.ps1 実行   ← コード更新ごと
       ↓
[4] GitHub Actions が自動ビルド（5〜10分）
       ↓
[5] APK をスマホにダウンロードしてインストール
```

---

## Step 1: GitHub アカウント（持っていない場合のみ）

ブラウザで <https://github.com/signup> を開き、無料アカウントを作成します。

- メールアドレス
- パスワード
- ユーザー名（半角英数）
- メール認証

の4ステップだけです。クレジットカード不要。

---

## Step 2: PCで GitHub にログイン

PowerShell を開いて以下を実行:

```powershell
gh auth login
```

対話に答えます:

| 質問 | 答え |
|---|---|
| What account do you want to log into? | **GitHub.com** |
| What is your preferred protocol for Git operations? | **HTTPS** |
| Authenticate Git with your GitHub credentials? | **Yes** |
| How would you like to authenticate? | **Login with a web browser** |

8桁のコードが表示されるのでコピー → 自動で開いたブラウザに貼り付け → 認証完了。

---

## Step 3: コードを GitHub にアップロード

プロジェクトのフォルダ（このREADMEがある場所）で:

```powershell
.\push-to-github.ps1
```

- 自動でリポジトリを作成
- 自動でコードをプッシュ
- 自動で Actions URL を表示

> 既定では **public（誰でも閲覧可）** で作成します。  
> 非公開にしたい場合: `.\push-to-github.ps1 -Private`

---

## Step 4: ビルド完了を待つ

GitHub Actions のページがブラウザで開きます（または手動で開く）。

- `Build Android APK` ワークフローが実行中（黄色の○）
- 5〜10分で完了（緑色の✓）

---

## Step 5: APK をダウンロード

1. 緑✓になったワークフローをクリック
2. ページ下部の **Artifacts** セクション
3. `aoi-rail-planner-debug-apk` をクリックしてダウンロード
4. ダウンロードした zip を展開 → `app-debug.apk` を取り出す

---

## Step 6: スマホにインストール

### 方法 A: クラウド経由（USB ケーブル不要）

1. Google ドライブ または Dropbox に APK をアップロード
2. スマホでそのアプリを開いて APK をダウンロード
3. スマホのファイルマネージャーで APK をタップ
4. 「**提供元不明のアプリ**」の許可を求められたら ON に
5. インストール

### 方法 B: メール添付

1. 自分宛にメールで APK を送信
2. スマホでメールを開いて添付ファイルをタップ
3. インストール

### 方法 C: USB ケーブル + adb（開発者向け）

```powershell
# Android Platform Tools が入っている場合
adb install app-debug.apk
```

---

## 起動確認

インストール後、アプリ一覧から **おまかせAIレールプランナー** をタップ。

確認項目:

| 画面 | 確認内容 |
|---|---|
| 年齢確認 | 「18歳以上です」をタップ → 在庫画面へ |
| 在庫画面 | + / - で個数調整、合計 / 100 表示 |
| カメラスキャン | 右上のカメラアイコン → カメラ権限を許可 → プレビュー表示 |
| レイアウト生成 | 直線×8、カーブ×16 を入れて「AIでレイアウト生成」 |

> ⚠️ **バックエンド未デプロイ時**: 「サーバーエラー」または「通信エラー」と表示されます（想定動作）。  
> バックエンドの Azure デプロイ手順は `README.md` 参照。

---

## トラブルシューティング

### `gh auth login` で「browser is not available」と出る

```powershell
gh auth login --web
```

または `--with-token` でパーソナルアクセストークン経由で認証してください。

### push 時に Permission denied

```powershell
gh auth refresh -h github.com -s repo,workflow
```

を実行してから再度プッシュ。

### Actions が失敗する（赤×）

1. 失敗したジョブをクリック
2. エラーログを確認
3. よくある原因:
   - `pubspec.yaml` の依存バージョン不整合 → 自動解決される場合が多い
   - Flutter create で `lib/` が上書きされた → ワークフローで対策済み
   - APK サイズ超過 → debug ビルドなので通常問題なし

### スマホでインストールできない

- 「不明なアプリのインストール」を ON にする必要があります
- 端末メーカーによってメニュー名が違います:
  - Pixel: 設定 → アプリ → 特別なアプリアクセス → 不明なアプリのインストール
  - Galaxy: 設定 → 生体認証とセキュリティ → 不明なアプリをインストール
  - Xperia: 設定 → アプリと通知 → 詳細設定 → 特別なアプリアクセス

---

## 次回以降の更新

コードを変更したら、もう一度 push するだけで再ビルドされます:

```powershell
git add -A
git commit -m "変更内容を1行で"
git push
```

→ Actions が自動起動 → 新しい APK が生成されます。
