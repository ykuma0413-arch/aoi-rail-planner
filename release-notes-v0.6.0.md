# MVP v0.6.0 - 本物の8の字 + 軽量署名版 (35MB)

## ⚠️ 重要: 必ず旧バージョンをアンインストールしてから入れてください
このバージョンから署名方式が変わったため（debug署名 → リリース署名）、
上書きインストールは必ず失敗します。

## ∞ 1. 本物の「8の字」コース生成
- 交差レールを中心に左ループ + 右ループを直交させる真の8の字
- 仕様の接続許容誤差 (10mm) を設計に組み込み、誤差4.24mmで厳密に成立
- 必要部材: 交差レール1 + カーブ24 + ハーフ直線4
- テーマ「8の字」で部材があれば必ず8の字、足りなければコンパクトな円にフォールバック
- 電車は交差部分をスーッと直交方向に渡る（経路ブリッジ実装）
- ※生成エンジンはサーバー側のため、バックエンドデプロイ後に有効

## 📦 2. 署名付きリリースビルド
- APK サイズ: 106MB → 35MB (1/3)
- R8 コード縮小 + リリース署名済み = Google Play 内部テストにそのまま提出可能
- 今後の全リリースはこの軽量版で配布

## 含まれる機能 (v0.5.0 から継続)
- 電車の走行アニメーション (3両編成・速度切替)
- じゅんばんに くみたてモード
- カメラスキャン実推論パイプライン (モデル差し替えで有効化)
