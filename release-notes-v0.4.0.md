# MVP v0.4.0 - 複雑なコース生成 + レール描画の完全統一

## 1. レイアウト生成エンジン v2.1（数式刷新）
- カーブの反転連結（右旋回）を導入
- 「対辺打ち消しの定理」により S字・波形入りの複雑な閉ループを厳密に生成
  - 基本ループの対辺に正味旋回角0の同一ピース列を挿入しても閉路が保たれる
- 自己交差は 20mm グリッド占有チェックで排除（不合格時は自動で形を変えて再試行）
- 閉路判定は仕様の許容誤差（距離10mm・角度2°）に準拠
- カーブ16本で100%成功の保証は維持（100回連続成功を再検証済み）
- カーブが16本より多いほど複雑な形（ピーナッツ形・波形ループ等）が出やすい
- ※エンジンはサーバー側のため、バックエンドデプロイ後に有効

## 2. シミュレーション描画を在庫アイコンと完全統一
- 道床の描画関数を在庫アイコンと物理的に共有（全く同じ形・同じ質感）
- 表示スケールを 1.7 倍拡大（レール1本 44px・道床 11px のずんぐり比率）
- 初期表示でコースが画面中央に表示（ピンチでズーム・パン可能）
- グリッドをレール1本分（106mm）間隔に変更
- 右旋回カーブの描画に対応

## インストール
旧バージョンをアンインストール後、APK をタップしてインストール。
